import React, { Fragment } from 'react';

function CustomInput({
    type,
    placeholder,
    icon,
    name,
}) {
    return (
        <Fragment>

        </Fragment>
    );
}

export default CustomInput;